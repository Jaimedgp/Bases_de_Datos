-- una lista es una tabla pblica -- la consulta realizada

-- si se definen Dec(5,1) y Dec(2,7) no son del mismo tipo --

-- varbinary(max) permite almacenar ficheros que no esten en la base de datos sino en el SO --

set identity_insert db_name ON -- permite insertar manualmente el identity

set identity_insert db_name off -- no permite insertar manualmente el identity

-- with nocheck hace que no se ejecute el check hasta ese punto -- 






